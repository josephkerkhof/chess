{
  "name": "@chess/server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "db:migration": "wrangler d1 migrations create DB",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:seed:local": "wrangler d1 execute DB --local --file=seed.sql",
    "db:open:local": "sqlite3 .wrangler/state/v3/d1/miniflare-D1DatabaseObject/[a-f0-9]*.sqlite"
  },
  "dependencies": {
    "chanfana": "^3.3.0",
    "hono": "^4.12.12",
    "uuid": "^14.0.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.9.5",
    "wrangler": "^4.126.0"
  }
}
